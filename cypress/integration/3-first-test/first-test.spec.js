/// <reference types="cypress" />

describe('Visit Google and do search', () => {
    beforeEach('Open Google website', () => {
        cy.visit('https://google.com');
    });

    it('', () => {

    });
})